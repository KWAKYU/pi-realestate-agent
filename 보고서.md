# 부동산 지도 AI 에이전트 — 프로젝트 보고서

> Pi · Skill · MCP · Pi Extension · Web UI 를 결합한 서울 아파트 실거래가 분석 에이전트

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 부동산 지도 AI 에이전트 (Pi Agent) |
| 선택 시나리오 | 부동산 실거래가 조회·분석 AI 에이전트 |
| 핵심 기술 | Pi(`@earendil-works/pi-coding-agent`), MCP(`@modelcontextprotocol/sdk`), OpenRouter, Express, Leaflet |
| 제출자 | (이름/학번 기입) |

---

## 1. 선택한 시나리오

**"부동산 실거래가 조회·분석 AI 에이전트"** 를 선택했다.

사용자가 지도 위에서 서울 아파트의 **국토교통부 실거래가**를 탐색하고, 자연어로 질문하면
AI 에이전트(Pi)가 실데이터를 직접 조회·계산해 답해 주는 서비스다. 단순한 LLM 호출이 아니라,
**Skill(분석 절차) · MCP(실데이터 도구) · Pi Extension(툴·커맨드)** 을 결합한 실제 서비스형 프로토타입으로 구현했다.

---

## 2. 문제 정의

- 국토교통부 실거래가 오픈 API(data.go.kr)는 **XML·법정동코드 기반**이라 일반 사용자가 직접 조회·해석하기 어렵다.
- 시세 확인 → 단지 비교 → 대출 한도 계산이 **여러 서비스에 흩어져** 있어 한 번에 묻고 답하기 힘들다.
- 기존 부동산 앱의 AI 챗봇은 대개 **고정 응답이거나 실데이터와 분리**되어 있어 "지금 이 동네 평균 얼마야?" 같은 실시간 질문에 약하다.

→ **지도 UI + 실거래가 + 자연어 AI 에이전트**를 하나로 묶어, 사용자가 묻기만 하면
에이전트가 도구(MCP)로 실데이터를 가져와 답하는 서비스가 필요하다.

---

## 3. 서비스 대상 사용자

- 서울 아파트 **매수·이사·투자**를 고민하는 일반 사용자
- 특정 지역의 **시세 수준과 최근 거래**를 빠르게 파악하고 싶은 사람
- **대출 가능 한도**(LTV/DSR)를 대략 가늠하려는 실수요자

---

## 4. 핵심 기능

1. **지도 기반 실거래가 탐색** — 지도를 드래그/줌하면 화면에 들어온 서울 구(區)의 실거래가가 가격 핀으로 **누적** 표시.
2. **필터·레이어** — 가격·면적 드롭다운(카드·마커 동시 필터), 지하철·학군·편의시설(OSM Overpass 실데이터)·개발계획 레이어.
3. **AI 에이전트 채팅(Pi)** — "강남구 제일 비싼 단지?", "연소득 8천이면 25억 대출 얼마?" 등 자연어 질문 →
   Pi 에이전트가 **MCP 툴로 실거래가 조회** + **대출 한도 계산** 후 한국어로 요약.
4. **AI 분석 리포트** — 로딩된 실데이터로 가격 분포·추천 TOP·단지 특성을 자동 집계.
5. **관심매물·검색·위성지도** 등 부가 기능.

---

## 5. 시스템 구조

### 5.1 구성도

```
[브라우저 Web UI]  부동산_지도_에이전트.html  (Leaflet 지도 + 채팅)
       │  POST /api/agent { message, hint }
       ▼
[Express 서버]  real-estate-agent/server.js
       │  child_process.spawn (헤드리스, stdin 닫음)
       ▼
[Pi 에이전트]  pi -p  (모델: OpenRouter)
   ├─ Skill      skills/real-estate-analysis.md     ← 분석 절차·응답 가이드 (자동 로드)
   └─ Extension  extensions/realestate.ts            ← registerTool / registerCommand / on(event)
            │  registerTool("search_transactions") = MCP 클라이언트
            ▼
[MCP 서버]  mcp-server/realestate-mcp.mjs  (@modelcontextprotocol/sdk, stdio)
            │  fetch (브라우저 User-Agent)
            ▼
[국토교통부 실거래가 API]  apis.data.go.kr
```

### 5.2 데이터 흐름 (채팅 1회)

1. 사용자가 채팅에 질문 입력 → 프론트엔드가 현재 지도에서 보는 구(區)를 `hint`로 함께 `/api/agent`에 POST.
2. 서버가 Pi CLI를 `--print` 모드로 **spawn**(헤드리스). 이때 **stdin을 닫아야** Pi가 입력 대기에 멈추지 않는다(아래 6.4 참고).
3. Pi가 프로젝트의 **Skill·Extension·AGENTS.md**를 자동 로드하고, 질문을 OpenRouter 모델로 추론.
4. 가격 질문이면 모델이 확장 툴 `search_transactions`를 호출 → 툴이 **MCP 클라이언트**로 MCP 서버에 연결 → MCP 서버가 data.go.kr 실거래가를 가져와 정리해 반환.
5. Pi가 결과를 요약해 stdout으로 출력 → 서버가 캡처해 JSON으로 응답 → 채팅에 렌더.

지도 핀·카드·분석 패널은 별도 경로(`/api/apt-trade`, data.go.kr 프록시)로 실데이터를 받아 렌더링한다.

---

## 6. Skill / MCP / Pi Extension 활용 방식

### 6.1 Pi 활용
- SDK: `@earendil-works/pi-coding-agent` (v0.78).
- 웹 서비스의 `/api/agent`가 Pi를 **헤드리스 CLI(`pi -p --mode text --provider openrouter`)** 로 구동한다.
- 모델 제공자는 **OpenRouter**(키는 `.env`의 `OPENROUTER_API_KEY`, 브라우저에 노출하지 않음).
- 별도로 `index.ts`는 동일 도구셋을 SDK로 구성한 TUI(대화형) 진입점이다.

### 6.2 Skill 활용 — `skills/real-estate-analysis.md`
- 표준 frontmatter(`name`, `description`)를 갖춘 Agent Skill.
- 내용: **"가격 질문 시 `search_transactions`로 실데이터 조회 → 평균/최고/최저 정리 → 필요 시 `loan_calculator`로 대출 계산"** 절차와 응답 스타일·주의사항(신고지연·참고용 명시)을 정의.
- Pi가 `package.json`의 `pi.skills` 경로에서 자동 로드해 시스템 프롬프트에 반영한다.

### 6.3 MCP 활용 — `mcp-server/realestate-mcp.mjs`
- `@modelcontextprotocol/sdk`의 `McpServer` + **stdio 트랜스포트**로 구현한 독립 MCP 서버.
- 노출 툴:
  - `search_transactions(region, ym?, rows?)` — 지역·월 실거래가(평균/최고/최저 + 단지 목록)
  - `market_trend(region, months?)` — 최근 N개월 평균가 추이
- 입력 스키마는 **Zod**로 정의. data.go.kr 호출 시 **브라우저 User-Agent**를 실어 WAF 차단을 우회(아래 6.4).
- 이 MCP 서버를 **Pi 확장이 MCP 클라이언트로 연결**해 호출하므로, MCP가 에이전트 추론 흐름 안에 통합되어 있다.

### 6.4 Pi Extension 활용 — `extensions/realestate.ts`
- `export default (pi) => { ... }` 형태의 ExtensionFactory. Pi가 `package.json`의 `pi.extensions` 경로에서 자동 로드.
- 등록 내용:
  - `pi.registerTool("search_transactions", …)` — **MCP 클라이언트**(`@modelcontextprotocol/sdk` Client + StdioClientTransport)로 MCP 서버에 연결해 실거래가 조회. (Extension ↔ MCP 연결 지점)
  - `pi.registerTool("loan_calculator", …)` — LTV 50% / DSR 40%(30년 원리금) 기준 예상 대출 한도 계산.
  - `pi.registerCommand("시세", …)` — `/시세` 슬래시 커맨드.
  - `pi.on("tool_execution_end", …)` — 툴 실행 이벤트 훅(로깅).
- `package.json` 매니페스트:
  ```json
  "keywords": ["pi-package"],
  "pi": { "extensions": ["./extensions"], "skills": ["./skills"] }
  ```

### 6.5 구현 중 해결한 핵심 이슈 (트러블슈팅)
- **data.go.kr WAF 차단:** 서버/curl 기본 요청(User-Agent 없음)은 400/403으로 차단됨. **브라우저 User-Agent**를 실으니 정상 응답. (실거래가 연동의 실제 원인)
- **응답 필드 불일치:** 실제 API는 영문 필드(`aptNm`, `dealAmount`, `umdNm`…)를 반환 → 파서를 영문 기준으로 수정.
- **Pi 헤드리스 stdin 멈춤:** `execFile`은 stdin을 열린 파이프로 둬 Pi가 입력 대기로 멈춤 → `spawn` + `stdio:['ignore',…]`로 stdin을 닫아 해결.
- **AGENTS.md 불일치:** 구 버전 AGENTS.md가 존재하지 않는 툴을 안내해 모델이 혼란 → 현재 확장 툴에 맞게 갱신.

---

## 7. Web UI 설명

- `부동산_지도_에이전트.html` — HTML/CSS/JS 단일 페이지 앱(별도 빌드 불필요). CLI가 아닌 **브라우저 화면**에서 입력·결과 확인.
- 좌측: Leaflet 지도(가격 핀·POI 레이어·필터). 우측: AI 에이전트 채팅창. 하단: 추천 매물 카드.
- 채팅 입력 → `/api/agent` 호출 → "🤖 에이전트 분석 중…" 표시 후 응답 렌더. 현재 보는 구를 힌트로 전달해 "이 동네"를 해석.
- 서버 미실행 시에도 내장 목업으로 UI는 동작(데모 가능), 실거래가·에이전트는 서버 필요.

---

## 8. 구현 결과

- **실거래가 연동 성공:** 강남구 2026.05 기준 약 266건, 평균 ≈ 28.9억 등 실데이터 정상 수신.
- **에이전트 동작 확인(웹 경로 `/api/agent`):**
  - "강남구 가장 비싼 단지" → `search_transactions` 호출 → "삼성동 아이파크 66.5억" 등 실데이터 응답.
  - "연소득 8000만원, 25억 아파트 대출?" → `loan_calculator` 호출 → "약 5.9억(DSR 40% 한도), 월 264만원, 참고용".
- **전체 체인 검증:** 브라우저 → `/api/agent` → Pi(Skill+Extension) → MCP 클라이언트 → MCP 서버 → data.go.kr 까지 실데이터가 흐름을 확인.
- **요구사항 충족:** Pi · Skill · MCP · Pi Extension · Web UI 5개 요소를 모두 한 서비스 안에서 동작시킴.

> (실행 화면 스크린샷: 지도+핀 / 필터 / AI 분석 리포트 / 채팅 응답 — 영상·이미지 첨부)

---

## 9. 한계점 및 개선 방향

| 한계 | 개선 방향 |
|---|---|
| 에이전트 응답이 **수십 초**(요청마다 Pi 콜드 스타트) | Pi **RPC 모드 상주 프로세스**로 전환해 세션 재사용 → 응답 단축 |
| 에이전트 호출이 **무상태**(멀티턴 메모리 없음) | 세션 유지/대화 기록 전달로 멀티턴 대화 지원 |
| 실거래가 API에 **위경도 없음** → 핀은 동(洞) 중심 근사 | 지오코딩(도로명주소 API) 연동으로 정확 좌표 |
| data.go.kr는 **매매만** 제공(전세·월세·오피스텔 미지원) | 전월세 실거래가 API 추가 연동 |
| 대출 계산은 **근사치**(LTV/DSR 단순화) | 규제지역·신용·기존대출 반영한 정교화 |
| `search_transactions` 기본 표본(rows) 한계로 최고가 단지가 표본에 빠질 수 있음 | rows 확대 또는 페이지네이션·정렬 파라미터 보강 |

---

### 부록 A. 실행 방법 요약
```bash
cd real-estate-agent
npm install
cp .env.example .env        # DATA_GO_KR_KEY, OPENROUTER_API_KEY 입력
node server.js              # http://localhost:3000
```

### 부록 B. 주요 파일
| 파일 | 역할 |
|---|---|
| `부동산_지도_에이전트.html` | Web UI (지도 + 채팅) |
| `real-estate-agent/server.js` | Express: `/api/agent`·`/api/chat`·`/api/apt-trade`·`/api/status` |
| `real-estate-agent/skills/real-estate-analysis.md` | **Skill** |
| `real-estate-agent/extensions/realestate.ts` | **Pi Extension** (툴·커맨드·훅 + MCP 클라이언트) |
| `real-estate-agent/mcp-server/realestate-mcp.mjs` | **MCP 서버** |
| `real-estate-agent/package.json` | Pi 매니페스트(`pi.extensions`, `pi.skills`) |
